# Sprint 1 验收纪要（S1-1/S1-2）（Inspector: Claude / 2026-06-11）

| Story | 结论 | 备注 |
|---|---|---|
| S1-1 转写升级 | 条件通过 | base 模型就绪（RTF 0.12）。原 DoD"20 词 <100ms"以 tiny 为参照系不成立（两模型互为参考无 ground truth），11/20 达标、中位 80ms。修订：以人工标注为准的精度验收延后至 golden EDL 标注时一并做 |
| S1-2 对齐模块 | 通过（独立复核） | 算法重写为全局峰值检测+NMS。重复检测 P=1.0 R=1.0（句9 NG 重拍捕获、伪重复 0、保留最后一次正确）；C1 版的 8 个伪重复已清零；句6 漏匹配已修复 |

## 独立复核数据（Inspector 直接读 s1-2_alignment.json）

20 句 / 17 匹配（85%）/ 1 重复句（2 区间，keep=最后一次）/ 15 脱稿区间 / CLI --help 正常。

## 待 Chen 确认：S1-2 DoD 修订案

原"句级匹配率 ≥95%"在即兴偏稿 115% 的素材上不合理。Dev 提议三层指标，Inspector 认可：

1. 全句匹配率 ≥80%（本期 85% ✓）
2. 关键句（核心数据/论点句，人工标注）匹配率 ≥95%
3. NG 重拍捕获率 100%（本期 1/1 ✓）——核心价值指标

未匹配的 3 句去向说明：它们不会被丢弃。对齐不上的素材区间进入"脱稿区间"池，在 S1-3 规则引擎中按声学规则（停顿/语气词）处理后默认保留——即兴发挥往往是好内容。

---

# S1-3/4/5 验收（追加 / 2026-06-11）

| Story | 结论 | 备注 |
|---|---|---|
| S1-3 规则引擎 | 通过 | 停顿残留 0、片尾 8.98s 空档剔除、句9 重拍丢弃正确、human 行保护验证通过。语气词残留 12（词级粗估，含句中正常用法）待 Chen 抽听后定夺 |
| S1-4 EDL+渲染 | 通过（经 D2 返工） | 首版 stream-copy 渲染超切 +23s 被验收打回；D2 改两步 seek+帧数截断+concat 修正，成片 177.367s vs EDL 177.360s（偏差 7ms），Inspector 复核时长与切点帧画面无异常 |
| S1-5 端到端 CLI | 通过 | roughcut.py run / --from-edl 断点续跑可用 |

成片：output/s1_roughcut_v1_precise.mp4（2:57，原素材 3:18，既有人工粗剪 2:34）。比人工版多 23s，来自脱稿区间全保留策略，是否裁剪由 S2 LLM 决策层处理。

Sprint 1 整体：达到 Sprint review 条件，待 Chen 审看成片（重点验 P1 重复漏识别是否根治；气口生硬属预期，S2 解决）。
